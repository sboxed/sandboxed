name: sandboxed_generator
description: "Code generator for Sandboxed components. Automatically creates boilerplate for registering and rendering UI elements in isolation."
resolution: workspace
version: 0.1.5+1
homepage: https://sboxed.dev/

environment:
  sdk: ">=3.5.4 <4.0.0"

# Add regular dependencies here.
dependencies:
  analyzer: ^7.0.0
  build: ^3.0.2
  code_builder: ^4.0.0
  collection: ^1.0.0
  crypto: ^3.0.0
  dart_style: ^3.0.0
  glob: ^2.0.0
  meta: ^1.0.0
  path: ^1.0.0
  source_gen: ^3.1.0
  yaml: ^3.0.0

  # utility
  recase: ^4.0.0
  source_span: ^1.0.0

  sandboxed_core: ^0.1.3+1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
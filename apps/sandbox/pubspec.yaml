name: sandboxed_sandbox
description: "Playgrount to test Sandboxed features"
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=3.5.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  sandboxed:
  sandboxed_core:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0
  
  build_runner: ^2.4.13
  sandboxed_generator:

flutter:
  uses-material-design: true

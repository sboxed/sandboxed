import 'package:flutter/material.dart';

class TaggedComponent extends StatelessWidget {
  const TaggedComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('This component has tags');
  }
}

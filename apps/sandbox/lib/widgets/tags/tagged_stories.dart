import 'package:flutter/material.dart';

class TaggedStories extends StatelessWidget {
  const TaggedStories({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('This component\'s stories has tags');
  }
}
